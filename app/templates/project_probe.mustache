{{>_probe_selector}}

<section id="visual">
  {{>_probe_meta}}

  {{#data}}
  <section class="graph">
    <h3>{{name}}</h3>

    <div class="bar"
        data-values="{{#values}}{{.}} {{/values}}">
    </div>
  </section>
  {{/data}}
</section>